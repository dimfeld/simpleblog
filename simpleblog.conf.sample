PostsDir = "testdata/posts"
DataDir = "testdata"
CacheDir = "cache"
TagsPath = "cache/tags.json"

IndexPosts = 15
TagsPageNewestFirst = true
ArchiveListNewestFirst = true

LogDir = "logs"

Domain = "localhost"
Port = 8080
# To bind to port 80, start as root and use the below to switch to
# a non-privileged user.
# RunAs = "bloguser"